.git
.claude
.template.config
TestResults
**/bin
**/obj
# Node: never ship the host's node_modules (wrong libc/arch), generated output or a stale build
# into the image context — the Dockerfile's Node stage runs a clean `npm ci` + `npm run build`,
# which regenerates artifacts/ (spec + client) from src/spec/main.tsp.
**/node_modules
artifacts
src/frontend/dist
